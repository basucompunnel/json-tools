import type { Metadata } from "next";
import FormatterPage from "@/components/formatter";

export const metadata: Metadata = {
  title: "JSON Formatter / Beautifier - JSON Tools",
  description: "Format and beautify your JSON with proper indentation. Make your JSON readable and well-structured instantly.",
};

export default FormatterPage;
