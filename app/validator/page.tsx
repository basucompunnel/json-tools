import type { Metadata } from "next";
import ValidatorPage from "@/components/validator";

export const metadata: Metadata = {
  title: "JSON Validator - JSON Tools",
  description: "Validate your JSON syntax and structure. Check if your JSON is valid and identify errors instantly.",
};

export default ValidatorPage;
