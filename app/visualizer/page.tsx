import type { Metadata } from "next";
import VisualizerPage from "@/components/visualizer";

export const metadata: Metadata = {
  title: "JSON Visualizer - JSON Tools",
  description:
    "Visualize JSON data in an interactive tree structure. Explore complex JSON hierarchies with expandable/collapsible nodes.",
};

export default VisualizerPage;
